import styled from 'styled-components';

export const Header = styled.header`
  display: flex;
  color: var(--blue-900);
  align-items: center;
  width: 100%;
  height: 50px;
  border-radius: 0px;
  background: white;

  .iconsdivide {
    p {
      display: flex;
      font-size: 1.5rem;
    }
    display: flex;
    align-items: center;
    gap: 10rem;
  }
`;
